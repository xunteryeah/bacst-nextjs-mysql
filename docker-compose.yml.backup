services:
  bacst-nextjs-mysql:
    build: .
    container_name: bacst-nextjs-mysql
    restart: unless-stopped


    ports:
      - "3000:3000"

    env_file:
      - path: .env
        required: false


    networks:
      - app-network

networks:
  app-network:
    driver: bridge


